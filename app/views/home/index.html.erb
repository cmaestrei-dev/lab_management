<script src="https://cdn.tailwindcss.com"></script>

<div class="bg-slate-50 min-h-screen p-8 font-sans text-slate-900">
  <div class="max-w-7xl mx-auto">

    <header class="flex flex-col md:flex-row md:items-center justify-between mb-10 gap-4">
      <div>
        <h1 class="text-3xl font-bold tracking-tight text-slate-800">Centro de Monitoreo de Integración</h1>
        <p class="text-slate-500 mt-1 text-sm">Estado operacional de laboratorios y contratos RESTful.</p>
      </div>

      <div class="flex items-center space-x-3">
        <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-emerald-100 text-emerald-800 border border-emerald-200 shadow-sm">
          <span class="w-2 h-2 mr-2 rounded-full bg-emerald-500"></span>
          API Status: Online
        </span>
        <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-800 border border-blue-200 shadow-sm">
          <span class="w-2 h-2 mr-2 rounded-full bg-blue-500 animate-pulse"></span>
          Webhooks: Active
        </span>
      </div>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10">
      <div class="bg-white p-6 rounded-xl border border-slate-200 shadow-sm">
        <p class="text-xs font-semibold text-slate-500 uppercase tracking-wider">Total Reportes</p>
        <h3 class="text-4xl font-bold mt-2 text-slate-800"><%= @total_reports_count %></h3>
        <p class="text-xs text-slate-400 mt-2 italic">Incidencias en plataforma</p>
      </div>

      <div class="bg-white p-6 rounded-xl border border-slate-200 shadow-sm border-t-4 border-t-orange-500">
        <p class="text-xs font-semibold text-orange-600 uppercase tracking-wider">Equipos en Fallo</p>
        <h3 class="text-4xl font-bold mt-2 text-orange-600"><%= @failing_computers_count %></h3>
        <p class="text-xs text-slate-400 mt-2 italic">PCs en mantenimiento</p>
      </div>

      <div class="bg-white p-6 rounded-xl border border-slate-200 shadow-sm">
        <p class="text-xs font-semibold text-slate-500 uppercase tracking-wider">Labs Saturados</p>
        <h3 class="text-4xl font-bold mt-2 text-slate-800"><%= @saturated_labs_count %></h3>
        <p class="text-xs text-slate-400 mt-2 italic">Sedes operando al límite</p>
      </div>
    </div>

    <div class="bg-white rounded-xl border border-slate-200 shadow-sm overflow-hidden">
      <div class="px-6 py-4 border-b border-slate-100 flex justify-between items-center bg-white">
        <h2 class="text-lg font-bold text-slate-800">Últimos Reportes de Incidencias</h2>
        <a href="/api/v1/reports" target="_blank" class="bg-slate-800 text-white px-4 py-2 rounded-lg text-xs font-semibold hover:bg-slate-700 transition-colors">
          Ver JSON de la API
        </a>
      </div>
      
      <div class="overflow-x-auto">
        <table class="w-full text-left">
          <thead class="bg-slate-50 border-b border-slate-200 uppercase text-[10px] font-bold text-slate-500 tracking-widest">
            <tr>
              <th class="px-6 py-4">Fecha</th>
              <th class="px-6 py-4">Profesor</th>
              <th class="px-6 py-4">Equipo (Serial)</th>
              <th class="px-6 py-4">Estado</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-slate-100 text-sm">
            <% @recent_reports.each do |report| %>
              <tr class="hover:bg-slate-50 transition-colors">
                <td class="px-6 py-4 text-slate-500 whitespace-nowrap">
                  <%= report.created_at.strftime("%Y-%m-%d") %>
                </td>
                <td class="px-6 py-4 font-medium text-slate-700">
                  <%= report.professor.name %>
                </td>
                <td class="px-6 py-4 text-slate-600">
                  <%= report.computer.serial_num %>
                </td>
                <td class="px-6 py-4">
                  <% color = case report.status 
                     when 'Resuelto' then 'bg-emerald-50 text-emerald-700 border-emerald-200'
                     when 'En Revisión' then 'bg-amber-50 text-amber-700 border-amber-200'
                     else 'bg-red-50 text-red-700 border-red-200'
                     end %>
                  <span class="px-2 py-1 rounded text-[10px] font-bold border <%= color %>">
                    <%= report.status.upcase %>
                  </span>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <div class="px-6 py-4 bg-slate-50 border-t border-slate-200 text-center text-xs text-slate-400 font-medium">
        Monitoreo en tiempo real procesado vía Sidekiq / SolidQueue.
      </div>
    </div>

  </div>
</div>